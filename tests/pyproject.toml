[project]
name = "server-cpp-tests"
version = "0.1.0"
description = "Test cases for server-cpp"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
